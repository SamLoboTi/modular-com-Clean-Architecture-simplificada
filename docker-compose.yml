services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: plansvel
      POSTGRES_USER: plansvel
      POSTGRES_PASSWORD: plansvel
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres_data:

